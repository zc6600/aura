# Standing Preferences

- Compute budget: CPU-only, single machine, seconds-to-minutes per experiment run. No GPU, no paid API calls inside any evaluated loop.
- No self-referential research (the framework researching itself) until explicitly reopened.
- Subfield for this run: multi-armed bandits / online learning, and adjacent problems (contextual bandits, non-stationary bandits, best-arm identification, regret minimization under structural assumptions).
- Recency window for literature: roughly the last 2 years (~2024-2026).
